#include <stdio.h>

int array[10] = {3, 4, 7, 2, 9, 15, 12, 1, 0, 4}

void swap(int i, int j);

int main(void) {
    int i = 0;
    for (i = 0; i < 10; i++) {
        int j = i + 1;
        for (j = i + 1; j < 10; j++) {
            if (array[j] > array[i]) {
                swap(i, j);
            } 
            /*if (newPR[j] == newPR[i]) {
                if (strcmp(collectURL[i], collectURL[j]) > 0) {
                    swap(i, j);
                }
            }*/
        }
    }
    
    for (int a = 0; a < 10; a++) {
        printf("%d\n", array[a]);
    }
    return 0;
    
}

void swap(int i, int j) {
    int temp = array[i];
    array[i] = array[j];
    array[j] = temp;
}
